#include <bits/stdc++.h>

using namespace std;

int main() {
	int tc;
	scanf("%d", &tc);
	while (tc--) {
		int n, c;
		scanf("%d %d", &n, &c);
		int l = 1;
		int r = 1e9;
		for (int i = 0; i < 60; ++i) {
			int m = l + r >> 1;
			
		}
	}
	return 0;
}