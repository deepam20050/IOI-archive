#include <bits/stdc++.h>

using namespace std;

const int N = 300010;

long long a[N], temp[N];
int cnt[N];

bool F(int x) {
	memset(temp, 0LL, sizeof temp);
	memset(cnt, 0, sizeof temp);
}

int main() {
	int t;
	scanf("%d", &t);
	while (t--) {
		int n, k, c;
		scanf("%d %d %d", &n, &k, &c);

	}
	return 0;
}