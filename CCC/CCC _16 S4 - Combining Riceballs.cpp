#include <bits/stdc++.h>

using namespace std;

int f (int l, int r) {
	if (l > r) 
}

int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; ++i) {
		scanf("%d", arr + i);
	}
	memset(dp, -1, sizeof dp);
	return 0;
}